image: alpine

stages:
  - build
  - test

cache:
  paths:
    - target/

maven-build:
  stage: build
  only:
  - master
  script:
    - mvn package; rm target/task2-1.0-SNAPSHOT.jar

bb-test:
  stage: test
  only:
  - master
  script:
    - proj_dir=`pwd`; (cd ~/code; ./checker.py -p ${proj_dir})
